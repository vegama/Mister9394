[pytest]
pythonpath = .
markers =
    calibration: batería de calibración del motor por competición y posición (B9-10). Se ejecuta con `pytest -m calibration`; simula partidos reales, así que tarda más que el resto.
    longitudinal: validación sintética multitemporada de 10 años; puede tardar decenas de segundos.
